import colors from './colors';

const common = {
  shadow: {
    shadowOffset: {
      width: 0,
      height: 4,
    },
    shadowColor: colors.shadow.popover,
    shadowOpacity: 0.15,
    shadowRadius: 8,
    elevation: 8,
  },
};

export default common;
